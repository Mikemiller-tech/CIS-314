# Tuples unmodifiable,indexed,ordered
# x = ("a',"b","c")
# x += tuple("c") 
# sets Modifiable,unindexed,unordered
# x.add("e") x.update(y) can also do x.update("e","f","g")
# remove() discard()
# clear() pop()
# list modifiable,indexed,ordered
# append() inset(4,"e")  x = a at 0 b at 1 c at 2 and d at 3
# updating values x[3] = "e" x.remove("a")
# pop() del x[0] x.clear()
# Dictionaries Modifiable,imdexed, ordered*, key->value
# print(x,keys()) get keys 
# print(x.get("instructer")) 
# adding items x["room"] = "Snyder 227"
# adding via update() x.update(("room": "Snyder 227"))
# update items x["name"] or updating via update x.update()
# pop() popitem()* clear() del()
# looping break continue else elif
#
#
#
#
#
#